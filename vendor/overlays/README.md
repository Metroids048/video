# Free provider overlays

After `npm run skills:vendor`, re-apply:

```bash
node scripts/run-python.mjs scripts/apply_free_provider_overlays.py
```

Or run `npm run skills:ensure` (includes overlay apply).
