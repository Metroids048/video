# Vendor

- `manifests/` — pin/source definitions for third-party video skills
- `repos/` — regenerable git work copies (gitignored); run `npm run skills:vendor`

Committed skill copies live in `../third_party_skills/` and sync to `.claude/skills` / `.agents/skills`.
