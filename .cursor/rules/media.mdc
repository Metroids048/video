---
# media.mdc — Cursor 媒体处理规则
description: 仅对媒体处理目录（renderers/, src/avs/render/, src/avs/ingest/）自动附加
globs:
  - "renderers/**"
  - "src/avs/render/**"
  - "src/avs/ingest/**"
  - "src/avs/qa/**"
  - "src/avs/delivery/**"
---

# 媒体处理规则

## 规格（与 tools-manifest.yaml 保持同步）

- 画布：1080×1920 px，30fps
- 编码：H.264（libx264）/ AAC / yuv420p
- 字幕安全区：距边缘至少 80px

## 输入安全

1. 所有输入先用 FFprobe 检查（时长、尺寸、fps、codec、音轨、可解码性）
2. 损坏文件不得进入渲染管道
3. 原始文件计算 SHA-256 并记录，处理后验证不变
4. 横屏素材明确使用 contain / cover / 布局模板，禁止静默拉伸

## FFmpeg 调用规则

- 所有 FFmpeg 命令检查退出码（非零 = 失败，记录并停止）
- 超时设置：单次渲染不超过 300 秒
- 输出路径使用相对路径
- 渲染前校验时间线无越界和重叠

## HyperFrames 集成

- 从 `timeline.json` 读取动效参数，不从聊天上下文获取
- 渲染失败时降级：FFmpeg 静态文字卡 + 记录 warning
- 不修改 `episode.json`，不管理 Episode 状态
- 动效任务必须先读 `third_party_skills/hyperframes`（见 `docs/video-plugin-routing.md`）

## 多渲染器（ADR-0006）

- Remotion / ChatCut / CapCut / video-use / OpenMontage 允许进入正式链路
- 开始前读 `docs/video-plugin-routing.md` 并加载对应 Skill
- 旁路产物必须回挂 Episode `work/` 或 `output/`；失败不得静默成功

## 其它视频插件（强制路由）

处理剪辑/成片/提示词/策略前，按 `docs/video-plugin-routing.md` 加载：
Remotion、video-use、Seedance、ChatCut、capcut-david、cut-skill、ip-strategist、openmontage。
禁止伪造 QA 状态。

## 音频

- 旁白优先，BGM ducking
- 无音轨时正常处理（不崩溃）
- 防止明显削波（峰值 < -1 dBFS）

## 测试要求

- 跨机器输出不要求逐像素一致
- 比较：可解码性、分辨率、帧率、时长容差、音轨存在
