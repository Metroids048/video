# ── Secrets & credentials ────────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.env
*.token
*.cookie
*.session
secrets/
credentials/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
pip-wheel-metadata/
*.egg-info/
dist/
build/
.eggs/
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Private IP strategy state (do not commit creator data or contracts)
ip-dossier.md
ip-dossier.md.bak
ip-contracts/
htmlcov/
.coverage
coverage.xml

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp
.pnp.js
.yarn/cache/
.yarn/unplugged/
.hyperframes/
renders/
renderers/hyperframes/**/snapshots/

# ── Episodes (all user inputs and generated work stay local) ─────────────────
episodes/inbox/*
episodes/active/*
episodes/completed/*
episodes/archived/*
!episodes/inbox/.gitkeep
!episodes/active/.gitkeep
!episodes/completed/.gitkeep
!episodes/archived/.gitkeep

# ── Project-level generated dirs ─────────────────────────────────────────────
cache/
logs/
output/

# YouTube research corpus (third-party transcripts/media/frames stay local)
workspace/research/youtube/

# ── Local runtimes and generated media (never commit) ───────────────────────
tools/local-tts/
work/
work_tmp_frames/
第一期视频_7x24自动交易/
封面.png
.mirasim/
.apodex/
docs/reference-research/workbuddy-samples/video-workshop/

# ── Vendor large repo work copies (regenerate via npm run skills:vendor) ─────
vendor/repos/

# ── Claude Code private settings ─────────────────────────────────────────────
.claude/settings.local.json

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Temp & scratch ────────────────────────────────────────────────────────────
*.tmp
*.bak
*.orig
scratch/

# Creative Loop 临时状态（每轮覆盖，不入库）
.avs-loop/

# ── VCI 独立仓库（video-content-intelligence 自带 .git，不进入 AVS 版本库）────
/video-content-intelligence/

# EP01 negative fixture keeps only compact review evidence; source media stays local.
tests/visual-regression/negative/EP-20260812-01-v1/source-full/
tests/visual-regression/negative/EP-20260812-01-v1/*.mp4
!tests/visual-regression/negative/EP-20260812-01-v1/v1-first-10s.mp4
